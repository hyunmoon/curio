// Compatibility import for existing personal tests/consumers. The page's
// initial preference is declarative; the filter calculation is shared.
export {visiblePoRepSectors} from './visibility.mjs';
